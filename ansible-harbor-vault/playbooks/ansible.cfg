[defaults]
# When the job cwd is this directory (common in AAP/AWX), Ansible only reads
# ansible.cfg from cwd — not ../ansible.cfg. Roles live next to playbooks/.
roles_path = ../roles
inventory = ../inventories/localhost.yml
host_key_checking = False
